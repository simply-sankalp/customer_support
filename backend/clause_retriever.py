def retrieve_relevant_clauses(query: str) -> list:
    # Placeholder — in real system, this uses vector search over PDF chunks
    return [
        "Clause C.1: Knee surgeries are covered if policy age is > 90 days.",
        "Table 3: Max payout for knee surgeries = ₹85,000"
    ]
